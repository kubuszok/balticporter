package com.badlogic.gdx.utils

class CharArrayTest {
  @org.junit.Test
  def constructorTest(): scala.Unit = {
    val array1: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    org.junit.Assert.assertEquals(0, array1.size)
    org.junit.Assert.assertTrue(array1.ordered)
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(100)
    org.junit.Assert.assertEquals(0, array2.size)
    org.junit.Assert.assertEquals(100, array2.capacity())
    org.junit.Assert.assertTrue(array2.ordered)
    val array3: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(false, 50)
    org.junit.Assert.assertEquals(0, array3.size)
    org.junit.Assert.assertEquals(50, array3.capacity())
    org.junit.Assert.assertFalse(array3.ordered)
    val array4: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array4.add('a')
    array4.add('b')
    val array5: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(array4)
    org.junit.Assert.assertEquals(2, array5.size)
    org.junit.Assert.assertEquals('a', array5.get(0))
    org.junit.Assert.assertEquals('b', array5.get(1))
    val chars: scala.Array[scala.Char] = scala.Array[scala.Char]('x', 'y', 'z')
    val array6: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(chars)
    org.junit.Assert.assertEquals(3, array6.size)
    org.junit.Assert.assertEquals('x', array6.get(0))
    org.junit.Assert.assertEquals('z', array6.get(2))
    val array7: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(true, chars, 1, 2)
    org.junit.Assert.assertEquals(2, array7.size)
    org.junit.Assert.assertEquals('y', array7.get(0))
    org.junit.Assert.assertEquals('z', array7.get(1))
    val array8: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("hello")
    org.junit.Assert.assertEquals(5, array8.size)
    org.junit.Assert.assertEquals("hello", array8.toString())
    val sb: java.lang.StringBuilder = new java.lang.StringBuilder("world")
    val array9: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(sb)
    org.junit.Assert.assertEquals(5, array9.size)
    org.junit.Assert.assertEquals("world", array9.toString())
  }
  @org.junit.Test
  def addTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.add('a')
    org.junit.Assert.assertEquals(1, array.size)
    org.junit.Assert.assertEquals('a', array.get(0))
    array.add('b', 'c')
    org.junit.Assert.assertEquals(3, array.size)
    org.junit.Assert.assertEquals('b', array.get(1))
    org.junit.Assert.assertEquals('c', array.get(2))
    array.add('d', 'e', 'f')
    org.junit.Assert.assertEquals(6, array.size)
    org.junit.Assert.assertEquals('f', array.get(5))
    array.add('g', 'h', 'i', 'j')
    org.junit.Assert.assertEquals(10, array.size)
    org.junit.Assert.assertEquals('j', array.get(9))
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array2.add('k')
    array2.add('l')
    array.addAll(array2)
    org.junit.Assert.assertEquals(12, array.size)
    org.junit.Assert.assertEquals('k', array.get(10))
    org.junit.Assert.assertEquals('l', array.get(11))
    array.addAll(scala.Array[scala.Char]('m', 'n', 'o'))
    org.junit.Assert.assertEquals(15, array.size)
    org.junit.Assert.assertEquals('o', array.get(14))
    val chars: scala.Array[scala.Char] = scala.Array[scala.Char]('p', 'q', 'r', 's', 't')
    array.addAll(chars, 1, 3)
    org.junit.Assert.assertEquals(18, array.size)
    org.junit.Assert.assertEquals('q', array.get(15))
    org.junit.Assert.assertEquals('r', array.get(16))
    org.junit.Assert.assertEquals('s', array.get(17))
  }
  @org.junit.Test
  def getSetTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.addAll(scala.Array[scala.Char]('a', 'b', 'c', 'd', 'e'))
    org.junit.Assert.assertEquals('a', array.get(0))
    org.junit.Assert.assertEquals('e', array.get(4))
    array.set(2, 'Z')
    org.junit.Assert.assertEquals('Z', array.get(2))
    array.set(0, 65.asInstanceOf[scala.Char].asInstanceOf[scala.Char])
    array.incr(0, 1.asInstanceOf[scala.Char].asInstanceOf[scala.Char])
    org.junit.Assert.assertEquals(66.asInstanceOf[scala.Char], array.get(0))
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array2.addAll(scala.Array[scala.Char](1.asInstanceOf[scala.Char].asInstanceOf[scala.Char], 2.asInstanceOf[scala.Char].asInstanceOf[scala.Char], 3.asInstanceOf[scala.Char].asInstanceOf[scala.Char]))
    array2.incr(10.asInstanceOf[scala.Char].asInstanceOf[scala.Char])
    org.junit.Assert.assertEquals(11.asInstanceOf[scala.Char], array2.get(0))
    org.junit.Assert.assertEquals(12.asInstanceOf[scala.Char], array2.get(1))
    org.junit.Assert.assertEquals(13.asInstanceOf[scala.Char], array2.get(2))
    array2.set(0, 5.asInstanceOf[scala.Char].asInstanceOf[scala.Char])
    array2.mul(0, 3.asInstanceOf[scala.Char].asInstanceOf[scala.Char])
    org.junit.Assert.assertEquals(15.asInstanceOf[scala.Char], array2.get(0))
    val array3: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array3.addAll(scala.Array[scala.Char](2.asInstanceOf[scala.Char].asInstanceOf[scala.Char], 3.asInstanceOf[scala.Char].asInstanceOf[scala.Char], 4.asInstanceOf[scala.Char].asInstanceOf[scala.Char]))
    array3.mul(2.asInstanceOf[scala.Char].asInstanceOf[scala.Char])
    org.junit.Assert.assertEquals(4.asInstanceOf[scala.Char], array3.get(0))
    org.junit.Assert.assertEquals(6.asInstanceOf[scala.Char], array3.get(1))
    org.junit.Assert.assertEquals(8.asInstanceOf[scala.Char], array3.get(2))
  }
  @org.junit.Test
  def removeTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(true, 10)
    array.addAll(scala.Array[scala.Char]('a', 'b', 'c', 'd', 'e'))
    org.junit.Assert.assertTrue(array.removeValue('c'))
    org.junit.Assert.assertEquals(4, array.size)
    org.junit.Assert.assertEquals('a', array.get(0))
    org.junit.Assert.assertEquals('b', array.get(1))
    org.junit.Assert.assertEquals('d', array.get(2))
    org.junit.Assert.assertEquals('e', array.get(3))
    org.junit.Assert.assertFalse(array.removeValue('z'))
    val removed: scala.Char = array.removeIndex(1)
    org.junit.Assert.assertEquals('b', removed)
    org.junit.Assert.assertEquals(3, array.size)
    org.junit.Assert.assertEquals('a', array.get(0))
    org.junit.Assert.assertEquals('d', array.get(1))
    org.junit.Assert.assertEquals('e', array.get(2))
    array.addAll(scala.Array[scala.Char]('f', 'g', 'h', 'i'))
    array.removeRange(1, 4)
    org.junit.Assert.assertEquals(3, array.size)
    org.junit.Assert.assertEquals('a', array.get(0))
    org.junit.Assert.assertEquals('h', array.get(1))
    org.junit.Assert.assertEquals('i', array.get(2))
    val unordered: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(false, 10)
    unordered.addAll(scala.Array[scala.Char]('a', 'b', 'c', 'd', 'e'))
    org.junit.Assert.assertTrue(unordered.removeValue('b'))
    org.junit.Assert.assertEquals(4, unordered.size)
    org.junit.Assert.assertEquals('a', unordered.get(0))
    org.junit.Assert.assertEquals('e', unordered.get(1))
    org.junit.Assert.assertEquals('c', unordered.get(2))
    org.junit.Assert.assertEquals('d', unordered.get(3))
    val toRemove: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    toRemove.addAll(scala.Array[scala.Char]('a', 'd'))
    org.junit.Assert.assertTrue(unordered.removeAll(toRemove))
    org.junit.Assert.assertEquals(2, unordered.size)
    org.junit.Assert.assertEquals('c', unordered.get(0))
    org.junit.Assert.assertEquals('e', unordered.get(1))
  }
  @org.junit.Test
  def searchTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.addAll(scala.Array[scala.Char]('h', 'e', 'l', 'l', 'o', ' ', 'w', 'o', 'r', 'l', 'd'))
    org.junit.Assert.assertTrue(array.contains('l'))
    org.junit.Assert.assertFalse(array.contains('z'))
    org.junit.Assert.assertEquals(0, array.indexOf('h'))
    org.junit.Assert.assertEquals(2, array.indexOf('l'))
    org.junit.Assert.assertEquals(-1, array.indexOf('z'))
    org.junit.Assert.assertEquals(9, array.lastIndexOf('l'))
    org.junit.Assert.assertEquals(7, array.lastIndexOf('o'))
    org.junit.Assert.assertEquals(-1, array.lastIndexOf('z'))
    org.junit.Assert.assertTrue(array.contains("hello"))
    org.junit.Assert.assertTrue(array.contains("world"))
    org.junit.Assert.assertFalse(array.contains("xyz"))
    org.junit.Assert.assertEquals(0, array.indexOf("hello"))
    org.junit.Assert.assertEquals(6, array.indexOf("world"))
    org.junit.Assert.assertEquals(-1, array.indexOf("xyz"))
    org.junit.Assert.assertEquals(0, array.lastIndexOf("hello"))
    org.junit.Assert.assertEquals(2, array.lastIndexOf("ll"))
  }
  @org.junit.Test
  def stackTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.add('a')
    array.add('b')
    array.add('c')
    org.junit.Assert.assertEquals('c', array.pop())
    org.junit.Assert.assertEquals(2, array.size)
    org.junit.Assert.assertEquals('b', array.peek())
    org.junit.Assert.assertEquals(2, array.size)
    org.junit.Assert.assertEquals('a', array.first())
    org.junit.Assert.assertTrue(array.notEmpty())
    org.junit.Assert.assertFalse(array.isEmpty())
    array.clear()
    org.junit.Assert.assertFalse(array.notEmpty())
    org.junit.Assert.assertTrue(array.isEmpty())
  }
  @org.junit.Test
  def arrayOperationsTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.addAll(scala.Array[scala.Char]('d', 'b', 'e', 'a', 'c'))
    array.sort()
    org.junit.Assert.assertEquals('a', array.get(0))
    org.junit.Assert.assertEquals('b', array.get(1))
    org.junit.Assert.assertEquals('c', array.get(2))
    org.junit.Assert.assertEquals('d', array.get(3))
    org.junit.Assert.assertEquals('e', array.get(4))
    array.swap(0, 4)
    org.junit.Assert.assertEquals('e', array.get(0))
    org.junit.Assert.assertEquals('a', array.get(4))
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array2.addAll(scala.Array[scala.Char]('1', '2', '3', '4', '5'))
    array2.reverse()
    org.junit.Assert.assertEquals('5', array2.get(0))
    org.junit.Assert.assertEquals('4', array2.get(1))
    org.junit.Assert.assertEquals('3', array2.get(2))
    org.junit.Assert.assertEquals('2', array2.get(3))
    org.junit.Assert.assertEquals('1', array2.get(4))
    array2.truncate(3)
    org.junit.Assert.assertEquals(3, array2.size)
    org.junit.Assert.assertEquals('5', array2.get(0))
    org.junit.Assert.assertEquals('4', array2.get(1))
    org.junit.Assert.assertEquals('3', array2.get(2))
    array2.clear()
    org.junit.Assert.assertEquals(0, array2.size)
  }
  @org.junit.Test
  def appendTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.append(true)
    org.junit.Assert.assertEquals("true", array.toString())
    array.clear()
    array.append(false)
    org.junit.Assert.assertEquals("false", array.toString())
    array.clear()
    (array.append: (scala.Char) => com.badlogic.gdx.utils.CharArray)('X')
    org.junit.Assert.assertEquals("X", array.toString())
    array.clear()
    (array.append: (scala.Int) => com.badlogic.gdx.utils.CharArray)(123)
    org.junit.Assert.assertEquals("123", array.toString())
    array.clear()
    (array.append: (scala.Int, scala.Int, scala.Char) => com.badlogic.gdx.utils.CharArray)(42, 5, '0')
    org.junit.Assert.assertEquals("00042", array.toString())
    array.clear()
    (array.append: (scala.Long) => com.badlogic.gdx.utils.CharArray)(9876543210L)
    org.junit.Assert.assertEquals("9876543210", array.toString())
    array.clear()
    (array.append: (scala.Float) => com.badlogic.gdx.utils.CharArray)(3.14f)
    org.junit.Assert.assertEquals("3.14", array.toString())
    array.clear()
    array.append(2.71828)
    org.junit.Assert.assertEquals("2.71828", array.toString())
    array.clear()
    array.append("Hello")
    org.junit.Assert.assertEquals("Hello", array.toString())
    array.append(" World")
    org.junit.Assert.assertEquals("Hello World", array.toString())
    array.clear()
    array.append(null.asInstanceOf[java.lang.String])
    org.junit.Assert.assertEquals("null", array.toString())
    array.clear()
    array.append("one")
    array.appendSeparator(',')
    array.append("two")
    array.appendSeparator(',')
    array.append("three")
    org.junit.Assert.assertEquals("one,two,three", array.toString())
    array.clear()
    val other: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("test")
    array.append(other)
    org.junit.Assert.assertEquals("test", array.toString())
    array.clear()
    val sb: java.lang.StringBuilder = new java.lang.StringBuilder("builder")
    array.append(sb)
    org.junit.Assert.assertEquals("builder", array.toString())
    array.clear()
    val sbuf: java.lang.StringBuffer = new java.lang.StringBuffer("buffer")
    array.append(sbuf)
    org.junit.Assert.assertEquals("buffer", array.toString())
  }
  @org.junit.Test
  def appendlnTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.appendln("Line 1")
    array.appendln("Line 2")
    array.append("Line 3")
    val result: java.lang.String = array.toString()
    org.junit.Assert.assertEquals("Line 1\nLine 2\nLine 3", result)
    array.clear()
    array.appendln()
    org.junit.Assert.assertEquals("\n", array.toString())
  }
  @org.junit.Test
  def paddingTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.append("Hello")
    array.appendPadding(5, '*')
    org.junit.Assert.assertEquals("Hello*****", array.toString())
    array.clear()
    array.appendFixedWidthPadLeft("42", 5, '0')
    org.junit.Assert.assertEquals("00042", array.toString())
    array.clear()
    array.appendFixedWidthPadLeft("12345", 3, '0')
    org.junit.Assert.assertEquals("345", array.toString())
    array.clear()
    array.appendFixedWidthPadRight("Hi", 5, ' ')
    org.junit.Assert.assertEquals("Hi   ", array.toString())
    array.clear()
  }
  @org.junit.Test
  def deleteTest(): scala.Unit = {
    var array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World!")
    array.delete(5, 11)
    org.junit.Assert.assertEquals("Hello!", array.toString())
    array.deleteCharAt(5)
    org.junit.Assert.assertEquals("Hello", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Hello World!")
    array.deleteAll('l')
    org.junit.Assert.assertEquals("Heo Word!", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Hello World!")
    array.deleteFirst('l')
    org.junit.Assert.assertEquals("Helo World!", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Hello World! Hello!")
    array.deleteAll("Hello")
    org.junit.Assert.assertEquals(" World! !", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Hello World! Hello!")
    array.deleteFirst("Hello")
    org.junit.Assert.assertEquals(" World! Hello!", array.toString())
  }
  @org.junit.Test
  def replaceTest(): scala.Unit = {
    var array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World!")
    org.junit.Assert.assertTrue(array.replaceFirst('l', 'L'))
    org.junit.Assert.assertEquals("HeLlo World!", array.toString())
    org.junit.Assert.assertFalse(array.replaceFirst('z', 'Z'))
    array = new com.badlogic.gdx.utils.CharArray("Hello World!")
    val count: scala.Int = array.replaceAll('l', 'L')
    org.junit.Assert.assertEquals(3, count)
    org.junit.Assert.assertEquals("HeLLo WorLd!", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Hello World!")
    array.replace(0, 5, "Hi")
    org.junit.Assert.assertEquals("Hi World!", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Hello World! Hello!")
    array.replaceAll("Hello", "Hi")
    org.junit.Assert.assertEquals("Hi World! Hi!", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Hello World! Hello!")
    array.replaceFirst("Hello", "Hi")
    org.junit.Assert.assertEquals("Hi World! Hello!", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("a-b-c")
    array.replace('-', " to ")
    org.junit.Assert.assertEquals("a to b to c", array.toString())
  }
  @org.junit.Test
  def insertTest(): scala.Unit = {
    var array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello!")
    (array.insert: (scala.Int, scala.Char) => scala.Unit)(5, ' ')
    org.junit.Assert.assertEquals("Hello !", array.toString())
    array.insert(6, "World")
    org.junit.Assert.assertEquals("Hello World!", array.toString())
    array.insert(0, "Say ")
    org.junit.Assert.assertEquals("Say Hello World!", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Value: ")
    array.insert(7, true)
    org.junit.Assert.assertEquals("Value: true", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("Number: ")
    (array.insert: (scala.Int, scala.Int) => com.badlogic.gdx.utils.CharArray)(8, 42)
    org.junit.Assert.assertEquals("Number: 42", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("AB")
    val chars: scala.Array[scala.Char] = scala.Array[scala.Char]('C', 'D', 'E')
    array.insert(1, chars)
    org.junit.Assert.assertEquals("ACDEB", array.toString())
    array = new com.badlogic.gdx.utils.CharArray("AC")
    array.insertRange(1, 2)
    array.set(1, 'B')
    array.set(2, 'B')
    org.junit.Assert.assertEquals("ABBC", array.toString())
  }
  @org.junit.Test
  def substringTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World!")
    org.junit.Assert.assertEquals("Hello", array.substring(0, 5))
    org.junit.Assert.assertEquals("World!", array.substring(6))
    org.junit.Assert.assertEquals("World", array.substring(6, 11))
    org.junit.Assert.assertEquals("Hello", array.leftString(5))
    org.junit.Assert.assertEquals("", array.leftString(0))
    org.junit.Assert.assertEquals("Hello World!", array.leftString(20))
    org.junit.Assert.assertEquals("World!", array.rightString(6))
    org.junit.Assert.assertEquals("", array.rightString(0))
    org.junit.Assert.assertEquals("Hello World!", array.rightString(20))
    org.junit.Assert.assertEquals("World", array.midString(6, 5))
    org.junit.Assert.assertEquals("", array.midString(6, 0))
    org.junit.Assert.assertEquals("World!", array.midString(6, 10))
  }
  @org.junit.Test
  def stringComparisonTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World")
    org.junit.Assert.assertTrue(array.startsWith("Hello"))
    org.junit.Assert.assertFalse(array.startsWith("World"))
    org.junit.Assert.assertTrue(array.startsWith(""))
    org.junit.Assert.assertTrue(array.endsWith("World"))
    org.junit.Assert.assertFalse(array.endsWith("Hello"))
    org.junit.Assert.assertTrue(array.endsWith(""))
    org.junit.Assert.assertTrue(array.contains("Hello"))
    org.junit.Assert.assertTrue(array.contains("World"))
    org.junit.Assert.assertTrue(array.contains(" "))
    org.junit.Assert.assertFalse(array.contains("xyz"))
    org.junit.Assert.assertTrue(array.containsIgnoreCase("hello"))
    org.junit.Assert.assertTrue(array.containsIgnoreCase("WORLD"))
    org.junit.Assert.assertFalse(array.containsIgnoreCase("xyz"))
    val other: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World")
    org.junit.Assert.assertTrue(array.equals(other))
    org.junit.Assert.assertTrue(array.equalsString("Hello World"))
    other.append("!")
    org.junit.Assert.assertFalse(array.equals(other))
    val upper: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("HELLO WORLD")
    org.junit.Assert.assertTrue(array.equalsIgnoreCase(upper))
    org.junit.Assert.assertTrue(array.equalsIgnoreCase("hello world"))
  }
  @org.junit.Test
  def charSequenceTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World!")
    org.junit.Assert.assertEquals(12, array.length())
    org.junit.Assert.assertEquals('H', array.charAt(0))
    org.junit.Assert.assertEquals('!', array.charAt(11))
    val sub: java.lang.CharSequence = array.subSequence(0, 5)
    org.junit.Assert.assertEquals("Hello", sub.toString())
  }
  @org.junit.Test
  def trimCapacityTest(): scala.Unit = {
    var array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(100)
    array.append("Hello")
    org.junit.Assert.assertEquals(100, array.capacity())
    org.junit.Assert.assertEquals(5, array.size)
    array.trim()
    org.junit.Assert.assertEquals("Hello", array.toString())
    org.junit.Assert.assertEquals(5, array.size)
    array = new com.badlogic.gdx.utils.CharArray(100)
    array.append("Test")
    array.trimToSize()
    org.junit.Assert.assertEquals(4, array.capacity())
    array = new com.badlogic.gdx.utils.CharArray("Hello")
    array.setLength(3)
    org.junit.Assert.assertEquals("Hel", array.toString())
    array.setLength(5)
    org.junit.Assert.assertEquals(5, array.length())
  }
  @org.junit.Test
  def hashCodeEqualsTest(): scala.Unit = {
    val array1: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello")
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello")
    val array3: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("World")
    org.junit.Assert.assertTrue(array1.equals(array2))
    org.junit.Assert.assertFalse(array1.equals(array3))
    org.junit.Assert.assertFalse(array1.equals(null))
    org.junit.Assert.assertFalse(array1.equals("Hello"))
    org.junit.Assert.assertEquals(array1.hashCode(), array2.hashCode())
    org.junit.Assert.assertNotEquals(array1.hashCode(), array3.hashCode())
  }
  @org.junit.Test
  def readerWriterTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World!")
    val reader: java.io.Reader = array.reader()
    val buffer: scala.Array[scala.Char] = new scala.Array[scala.Char](5)
    val read: scala.Int = reader.read(buffer)
    org.junit.Assert.assertEquals(5, read)
    org.junit.Assert.assertArrayEquals(scala.Array[scala.Char]('H', 'e', 'l', 'l', 'o'), buffer)
    org.junit.Assert.assertEquals(' ', reader.read())
    org.junit.Assert.assertEquals('W', reader.read())
    reader.skip(2)
    org.junit.Assert.assertEquals('l', reader.read())
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    val writer: java.io.Writer = array2.writer()
    writer.write("Test")
    org.junit.Assert.assertEquals("Test", array2.toString())
    writer.write(' ')
    writer.write(scala.Array[scala.Char]('1', '2', '3'))
    org.junit.Assert.assertEquals("Test 123", array2.toString())
  }
  @org.junit.Test
  def unicodeTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    val smiley: scala.Int = 128512
    array.appendCodePoint(smiley)
    org.junit.Assert.assertEquals(2, array.size)
    val cp: scala.Int = array.codePointAt(0)
    org.junit.Assert.assertEquals(smiley, cp)
    array.append("Hello")
    val count: scala.Int = array.codePointCount(0, array.size)
    org.junit.Assert.assertEquals(6, count)
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array2.appendCodePoint(128512)
    array2.append("Hi")
    array2.reverseCodePoints()
    org.junit.Assert.assertEquals("iH", array2.substring(0, 2))
  }
  @org.junit.Test
  def iteratorTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    val list: scala.collection.mutable.ArrayBuffer[java.lang.String] = new scala.collection.mutable.ArrayBuffer[java.lang.String]()
    list += "One"
    list += "Two"
    list += "Three"
    array.appendAll(balticporter.runtime.JavaIterable.from(list))
    org.junit.Assert.assertEquals("OneTwoThree", array.toString())
    array.clear()
    array.appendWithSeparators(balticporter.runtime.JavaIterable.from(list), ", ")
    org.junit.Assert.assertEquals("One, Two, Three", array.toString())
    array.clear()
    val iter: balticporter.runtime.JavaIterator[java.lang.String] = balticporter.runtime.JavaIterator.from(list.iterator)
    array.appendAll(iter)
    org.junit.Assert.assertEquals("OneTwoThree", array.toString())
    array.clear()
    val strArray: scala.Array[java.lang.String] = scala.Array[java.lang.String]("A", "B", "C")
    array.appendWithSeparators(strArray.asInstanceOf[scala.Array[java.lang.Object]], "-")
    org.junit.Assert.assertEquals("A-B-C", array.toString())
  }
  @org.junit.Test
  def edgeCasesTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    org.junit.Assert.assertEquals(-1, array.indexOf('a'))
    org.junit.Assert.assertEquals(-1, array.lastIndexOf('a'))
    org.junit.Assert.assertFalse(array.contains('a'))
    org.junit.Assert.assertFalse(array.removeValue('a'))
    array.append("")
    org.junit.Assert.assertEquals(0, array.size)
    array.append(null.asInstanceOf[java.lang.String])
    org.junit.Assert.assertEquals("null", array.toString())
    val large: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray(10000);
    { var i: scala.Int = 0; while (i < 10000) { {
      large.add(('A' + (i % 26)).asInstanceOf[scala.Char].asInstanceOf[scala.Char])
    }; i = i + 1 } }
    org.junit.Assert.assertEquals(10000, large.size)
    try {
      array.get(100)
      org.junit.Assert.fail("Should throw exception")
    } catch {
      case e: java.lang.IndexOutOfBoundsException => {
        ()
      }
    }
    try {
      array.set(100, 'x')
      org.junit.Assert.fail("Should throw exception")
    } catch {
      case e: java.lang.IndexOutOfBoundsException => {
        ()
      }
    }
  }
  @org.junit.Test
  def toArrayTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.addAll(scala.Array[scala.Char]('a', 'b', 'c', 'd', 'e'))
    val chars: scala.Array[scala.Char] = array.toCharArray()
    org.junit.Assert.assertArrayEquals(scala.Array[scala.Char]('a', 'b', 'c', 'd', 'e'), chars)
    val target: scala.Array[scala.Char] = new scala.Array[scala.Char](10)
    array.getChars(1, 4, target, 2)
    org.junit.Assert.assertEquals('b', target(2))
    org.junit.Assert.assertEquals('c', target(3))
    org.junit.Assert.assertEquals('d', target(4))
  }
  @org.junit.Test
  def drainTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World!")
    val drained: scala.Char = array.drainChar(6)
    org.junit.Assert.assertEquals('W', drained)
    org.junit.Assert.assertEquals("Hello orld!", array.toString())
    val target: scala.Array[scala.Char] = new scala.Array[scala.Char](5)
    val count: scala.Int = array.drainChars(0, 5, target, 0)
    org.junit.Assert.assertEquals(5, count)
    org.junit.Assert.assertArrayEquals(scala.Array[scala.Char]('H', 'e', 'l', 'l', 'o'), target)
    org.junit.Assert.assertEquals(" orld!", array.toString())
  }
  @org.junit.Test
  def appendSeparatorTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.appendSeparator(',')
    array.append("first")
    org.junit.Assert.assertEquals("first", array.toString())
    array.appendSeparator(',')
    array.append("second")
    org.junit.Assert.assertEquals("first,second", array.toString())
    val array2: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    (array2.appendSeparator: (scala.Char, scala.Char) => com.badlogic.gdx.utils.CharArray)(',', ';')
    org.junit.Assert.assertEquals(";", array2.toString())
    val array3: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray();
    { var i: scala.Int = 0; while (i < 3) { {
      array3.appendSeparator(',', i)
      array3.append("item" + i)
    }; i = i + 1 } }
    org.junit.Assert.assertEquals("item0,item1,item2", array3.toString())
    val array4: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array4.append("A")
    array4.appendSeparator(" | ")
    array4.append("B")
    array4.appendSeparator(" | ")
    array4.append("C")
    org.junit.Assert.assertEquals("A | B | C", array4.toString())
  }
  @org.junit.Test
  def randomTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.addAll(scala.Array[scala.Char]('a', 'b', 'c', 'd', 'e'))
    val random: scala.Char = array.random()
    org.junit.Assert.assertTrue(array.contains(random))
    array.shuffle()
    org.junit.Assert.assertEquals(5, array.size)
    org.junit.Assert.assertTrue(array.contains('a'))
    org.junit.Assert.assertTrue(array.contains('b'))
    org.junit.Assert.assertTrue(array.contains('c'))
    org.junit.Assert.assertTrue(array.contains('d'))
    org.junit.Assert.assertTrue(array.contains('e'))
  }
  @org.junit.Test
  def appendToTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello World")
    val sb: java.lang.StringBuilder = new java.lang.StringBuilder("Start: ")
    array.appendTo(sb)
    org.junit.Assert.assertEquals("Start: Hello World", sb.toString())
    val sbuf: java.lang.StringBuffer = new java.lang.StringBuffer("Start: ")
    array.appendTo(sbuf)
    org.junit.Assert.assertEquals("Start: Hello World", sbuf.toString())
  }
  @org.junit.Test
  def setCharAtTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Hello")
    array.setCharAt(1, 'a')
    org.junit.Assert.assertEquals("Hallo", array.toString())
    array.setCharAt(2, 'p').setCharAt(3, 'p').setCharAt(4, 'y')
    org.junit.Assert.assertEquals("Happy", array.toString())
  }
  @org.junit.Test
  def toStringAndClearTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray("Test String")
    val result: java.lang.String = array.toStringAndClear()
    org.junit.Assert.assertEquals("Test String", result)
    org.junit.Assert.assertEquals(0, array.size)
    org.junit.Assert.assertTrue(array.isEmpty())
  }
  @org.junit.Test
  def toStringWithSeparatorTest(): scala.Unit = {
    val array: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    array.addAll(scala.Array[scala.Char]('a', 'b', 'c', 'd', 'e'))
    val result: java.lang.String = array.toString(",")
    org.junit.Assert.assertEquals("a,b,c,d,e", result)
    val empty: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    org.junit.Assert.assertEquals("", empty.toString(","))
    val single: com.badlogic.gdx.utils.CharArray = new com.badlogic.gdx.utils.CharArray()
    single.add('x')
    org.junit.Assert.assertEquals("x", single.toString(","))
  }
}